/* import types from './types';

const Reducer = (action, state = {}) => {
  switch (action.type) {
    case types.in:
      return {
        state: true,
      };
    case types.out:
      return {
        state: false,
      };
    default:
      return state;
  }
};

export default Reducer;
 */